---
phase: 03-model-wide-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl
  - tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Products.tmdl
autonomous: true
requirements:
  - AUD-03
  - AUD-04

must_haves:
  truths:
    - "Running /pbi:audit against the TMDL fixture detects a date table (AUD-04 positive case)"
    - "Running /pbi:audit against the TMDL fixture detects an isolated table with no outbound relationships (AUD-03 WARN case)"
  artifacts:
    - path: "tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl"
      provides: "TMDL date table fixture with dataCategory: Time at table level"
      contains: "dataCategory: Time"
    - path: "tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Products.tmdl"
      provides: "Isolated table with no outbound relationships for heuristic WARN testing"
  key_links:
    - from: "tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl"
      to: "pbi-audit SKILL.md Domain C: Date Table"
      via: "audit reads table-level dataCategory: Time property"
      pattern: "dataCategory: Time"
    - from: "tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Products.tmdl"
      to: "pbi-audit SKILL.md Domain A: Relationships R-02"
      via: "audit checks outbound relationships for tables matching fact-like naming patterns"
      pattern: "Products.*no.*relationship"
---

<objective>
Create two missing TMDL test fixtures that cover the Wave 0 gaps identified in 03-VALIDATION.md.

Purpose: The existing Sales.tmdl fixture already exercises bidirectional relationship (AUD-03 CRITICAL) and missing description (AUD-05 INFO). Two cases are untestable without new fixtures: (1) date table detected correctly (AUD-04 positive case), and (2) isolated table with no outbound relationships (AUD-03 WARN heuristic). These fixtures must exist before the audit skill is written so that all requirements can be verified end-to-end.

Output: Date.tmdl (date table marked with dataCategory: Time, with a DateTime date column) and Products.tmdl (simple dimension table with no relationships to anything in the model, named to trigger the fact-table heuristic check).
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-model-wide-audit/03-CONTEXT.md
@.planning/phases/03-model-wide-audit/03-RESEARCH.md
@.planning/phases/03-model-wide-audit/03-VALIDATION.md

<interfaces>
<!-- Existing fixture for reference — TMDL table structure pattern -->
<!-- From tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Sales.tmdl -->

```tmdl
table Sales

	column Date
		dataType: dateTime
		formatString: General Date

	column Amount
		dataType: decimal
		formatString: #,##0.00

	/// Year-to-date revenue filtered to visible periods.
	measure 'Revenue YTD' =
			CALCULATE([Revenue], DATESYTD('Date'[Date]))
		formatString: #,##0
		displayFolder: "Time Intelligence"

	measure Revenue =
			SUM(Sales[Amount])
		formatString: #,##0
		displayFolder: "Base Measures"
```

<!-- Existing relationships fixture for reference -->
<!-- From tests/fixtures/pbip-tmdl/.SemanticModel/definition/relationships.tmdl -->

```tmdl
relationship 'Sales_Date'
	fromTable: Sales
	fromColumn: Date
	toTable: Date
	toColumn: Date
	crossFilteringBehavior: bothDirections
```

<!-- IMPORTANT: dataCategory: Time is a TABLE-LEVEL property in TMDL.
     It appears as an indented line after the table declaration,
     BEFORE any column or measure blocks. This is distinct from
     column-level dataCategory (e.g. dataCategory: PaddedDateTableDates)
     which appears inside a column block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Date.tmdl fixture with table-level dataCategory: Time</name>
  <files>tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl</files>
  <action>
    Create a TMDL table file for a Date dimension table. Requirements:

    1. Table declaration: `table Date`
    2. Table-level property on the NEXT LINE (indented with one tab): `dataCategory: Time`
       — This must appear BEFORE any column blocks. This is the property the audit will detect for AUD-04.
    3. Include one column named `Date` with dataType: dateTime — this is the date key column.
    4. Include a second column `Year` with dataType: int64 for realism.
    5. No measures (date tables typically have no measures).

    The file MUST have `dataCategory: Time` as a table-level indented property (same indentation as `column` declarations), not inside a column block. Refer to the TMDL syntax in the research notes: table-level properties appear immediately after the table declaration line, before the first `column` keyword.

    The existing relationships.tmdl already has Sales → Date (toTable: Date, toColumn: Date), so this fixture must be named `Date.tmdl` to match — confirming the relationship is valid.

    Use one tab character for indentation (matching the Sales.tmdl convention).
  </action>
  <verify>
    <automated>grep -n "dataCategory: Time" "/c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl" && echo "PASS: dataCategory: Time found in Date.tmdl"</automated>
  </verify>
  <done>Date.tmdl exists with `dataCategory: Time` as a table-level property (line appears before any column block) and a `Date` column of type dateTime.</done>
</task>

<task type="auto">
  <name>Task 2: Create Products.tmdl fixture — isolated table with no outbound relationships</name>
  <files>tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Products.tmdl</files>
  <action>
    Create a minimal TMDL table file for a Products dimension table. Requirements:

    1. Table declaration: `table Products`
    2. Two columns: `ProductID` (dataType: int64) and `ProductName` (dataType: string)
    3. No measures, no dataCategory (it is not a date table)
    4. NO corresponding entry in relationships.tmdl — leave that file untouched

    Purpose: The audit Domain A Rule R-02 uses a heuristic to detect potential fact or dimension tables with no outbound relationships. "Products" matches a typical dimension table naming pattern that the audit should flag as WARN ("verify this table is intentionally isolated"). By having Products in the tables directory but NO relationship referencing it in relationships.tmdl, the audit rule fires.

    Do NOT modify relationships.tmdl — the Products table must remain unconnected.

    Use one tab character for indentation (matching the Sales.tmdl convention).
  </action>
  <verify>
    <automated>ls "/c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Products.tmdl" 2>/dev/null && echo "PASS: Products.tmdl exists" && grep -c "Products" "/c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl/.SemanticModel/definition/relationships.tmdl" 2>/dev/null && echo "FAIL: Products appears in relationships" || echo "PASS: Products not in relationships"</automated>
  </verify>
  <done>Products.tmdl exists with at least two columns and NO entry in relationships.tmdl — table is intentionally isolated.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Three table files now exist under `tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/`: Sales.tmdl, Date.tmdl, Products.tmdl
2. `grep "dataCategory: Time" tests/fixtures/pbip-tmdl/.SemanticModel/definition/tables/Date.tmdl` returns a match
3. `grep "Products" tests/fixtures/pbip-tmdl/.SemanticModel/definition/relationships.tmdl` returns NO match (Products is isolated)
4. relationships.tmdl is unchanged — it still has only the Sales → Date relationship
</verification>

<success_criteria>
- Date.tmdl: table-level `dataCategory: Time` present before any column block, Date column is dateTime type
- Products.tmdl: simple two-column table, no relationship referencing it in relationships.tmdl
- Both files follow TMDL indentation convention (one tab) matching Sales.tmdl
- No other existing fixtures modified
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-wide-audit/03-01-SUMMARY.md`
</output>
