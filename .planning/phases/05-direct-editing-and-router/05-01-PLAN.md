---
phase: 05-direct-editing-and-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi/SKILL.md
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Bare /pbi with no inline text shows a 4-category menu and waits for selection"
    - "Category A selection triggers a follow-up DAX command question (explain/format/optimise/comment)"
    - "Category B routes directly to /pbi:audit with no follow-up question"
    - "Category C triggers a follow-up question distinguishing diff and commit"
    - "Category D routes directly to /pbi:edit with no follow-up question"
    - "/pbi [intent] with inline text routes directly to the correct subcommand without showing the menu"
  artifacts:
    - path: ".claude/skills/pbi/SKILL.md"
      provides: "Bare /pbi router skill"
      contains: "category menu"
  key_links:
    - from: ".claude/skills/pbi/SKILL.md"
      to: "existing subcommands"
      via: "routing instructions referencing /pbi:explain, /pbi:format, /pbi:optimise, /pbi:comment, /pbi:audit, /pbi:diff, /pbi:commit, /pbi:error, /pbi:edit, /pbi:load"
      pattern: "/pbi:(explain|format|optimise|comment|audit|diff|commit|error|edit|load)"
---

<objective>
Create the bare /pbi router skill — a lightweight conversational orchestrator that orients analysts to the full command suite and routes their intent to the correct subcommand.

Purpose: Analysts can type /pbi at any entry point in their workflow and either describe what they need (direct routing) or browse a category menu to discover the right command.
Output: .claude/skills/pbi/SKILL.md
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-direct-editing-and-router/05-CONTEXT.md
@.planning/phases/05-direct-editing-and-router/05-RESEARCH.md

<interfaces>
<!-- Existing skill frontmatter pattern — replicate for pbi router -->

From .claude/skills/pbi-comment/SKILL.md (lines 1-6):
```yaml
---
name: pbi-comment
description: Add inline // comments to a DAX measure and generate a Description field value for Power BI. Use when an analyst asks to comment, document, or annotate a DAX measure.
disable-model-invocation: true
model: sonnet
allowed-tools: Read, Write, Bash
---
```

Router variant (no bash, no file I/O):
```yaml
---
name: pbi
description: Power BI skill suite entry point. Routes to the appropriate /pbi subcommand based on analyst intent. Use when an analyst types /pbi with or without a description of what they need.
disable-model-invocation: true
model: sonnet
allowed-tools: Read
---
```
Note: allowed-tools: Read used (not empty array) to avoid SKILL.md schema parse error — confirmed fallback from RESEARCH.md open question #1.

All 9 existing subcommands for the menu:
- /pbi:explain, /pbi:format, /pbi:optimise, /pbi:comment (DAX group A)
- /pbi:audit (Audit group B)
- /pbi:diff, /pbi:commit (Changes group C)
- /pbi:edit (Edit group D)
- /pbi:load (accessible via inline intent "load")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pbi router skill</name>
  <files>.claude/skills/pbi/SKILL.md</files>
  <action>
Create `.claude/skills/pbi/SKILL.md` (directory must be created first — `mkdir -p .claude/skills/pbi`).

Frontmatter (per research Pattern 9 and open question #1 resolved):
```yaml
---
name: pbi
description: Power BI skill suite entry point. Routes to the appropriate /pbi subcommand based on analyst intent. Use when an analyst types /pbi with or without a description of what they need.
disable-model-invocation: true
model: sonnet
allowed-tools: Read
---
```

NO bash injection blocks. This is a pure conversational routing skill.

Body — two sections:

## Routing Logic

If `$ARGUMENTS` is non-empty (analyst typed `/pbi [some intent]`):

Read the inline text as analyst intent. Map to the most relevant subcommand using this keyword table:
- "explain" / "what does" / "understand" / "how does" → /pbi:explain
- "format" / "clean up" / "prettify" / "style" → /pbi:format
- "optimise" / "optimize" / "performance" / "speed up" / "slow" → /pbi:optimise
- "comment" / "annotate" / "document" / "describe" → /pbi:comment
- "audit" / "health check" / "review model" / "find issues" → /pbi:audit
- "diff" / "what changed" / "changes" / "show changes" → /pbi:diff
- "commit" / "save" / "snapshot" / "git" → /pbi:commit
- "error" / "fix" / "diagnose" / "broken" / "failing" → /pbi:error
- "edit" / "rename" / "update" / "change" / "modify" / "add measure" → /pbi:edit
- "load" / "context" / "model context" / "load project" → /pbi:load

Output: "Routing to /pbi:[subcommand] — [one-line description of what it does]."
Then immediately behave as if the analyst had invoked /pbi:[subcommand] directly (carry through any inline arguments that follow the intent keyword as if they were passed to the subcommand).

If intent is ambiguous between two commands: pick the most specific match and note it — "Routing to /pbi:edit (you can also use /pbi:comment if you only need to add comments)."

## Category Menu (bare /pbi)

If `$ARGUMENTS` is empty or absent:

Output the following category menu exactly:

---
What would you like to do?

**A — Work on a DAX measure**
  explain · format · optimise · comment

**B — Audit the model**
  audit

**C — See or commit changes**
  diff · commit

**D — Edit a model file**
  edit

Type A, B, C, or D — or describe what you need and I'll route you directly.

---

On analyst response:

- "A": Ask — "Which DAX command? **explain** · **format** · **optimise** · **comment**" — then on selection route to the chosen /pbi:DAX subcommand.
- "B": Route directly to /pbi:audit. Output "Routing to /pbi:audit — running a full model health check." then proceed as /pbi:audit.
- "C": Ask — "Which command? **diff** — see what changed · **commit** — save a snapshot" — then on selection route to the chosen subcommand.
- "D": Route directly to /pbi:edit. Output "Routing to /pbi:edit — describe the model change you want to make." then proceed as /pbi:edit.
- Free-text response: Apply the intent mapping from the Routing Logic section above and route directly.
- Any response that does not match A/B/C/D or a recognisable intent keyword: Output "I didn't catch that — type A, B, C, or D, or describe what you need."
  </action>
  <verify>
    <automated>ls /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi/SKILL.md && grep -c "category menu\|Routing Logic\|Work on a DAX measure\|Audit the model\|See or commit changes\|Edit a model file" /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi/SKILL.md</automated>
  </verify>
  <done>
- .claude/skills/pbi/SKILL.md exists with correct frontmatter (name: pbi, disable-model-invocation: true, model: sonnet, allowed-tools: Read)
- File contains both routing sections: inline $ARGUMENTS routing and category menu
- Category menu has all 4 groups with correct subcommand lists
- Intent keyword table covers all 10 subcommands including /pbi:load
- Follow-up questions defined for A and C; direct routing defined for B and D
  </done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Run bare `/pbi` — verify category menu appears with groups A/B/C/D
2. Type "A" — verify follow-up question "Which DAX command? explain · format · optimise · comment" appears
3. Type "explain" — verify routes to /pbi:explain behaviour
4. Run `/pbi explain this measure` — verify direct route to /pbi:explain without menu
5. Run `/pbi audit` — verify direct route to /pbi:audit without menu
6. Type "B" from menu — verify routes directly to /pbi:audit
7. Type "D" from menu — verify routes directly to /pbi:edit
</verification>

<success_criteria>
- Bare /pbi invocation shows 4-category menu
- Inline intent (/pbi [text]) routes to correct subcommand without showing menu
- Two-step follow-up for A and C; single-step for B and D
- All 10 subcommands reachable through the router
- SKILL.md file exists at .claude/skills/pbi/SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/05-direct-editing-and-router/05-01-SUMMARY.md`
</output>
