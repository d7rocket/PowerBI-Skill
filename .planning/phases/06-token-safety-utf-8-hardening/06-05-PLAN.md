---
phase: 06-token-safety-utf-8-hardening
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - .claude/skills/pbi/commands/load.md
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02

must_haves:
  truths:
    - "load.md TMSL branch contains a chunked-read guard for large model.bim files"
    - "Running /pbi load on a large TMSL model does not cause token overflow — executor is instructed to chunk"
  artifacts:
    - path: ".claude/skills/pbi/commands/load.md"
      provides: "TMSL chunked read guard so large model.bim files are read in offset/limit chunks"
      contains: "offset/limit"
  key_links:
    - from: "load.md Step 2 TMSL branch"
      to: "Read tool offset/limit parameters"
      via: "explicit instruction: if model.bim >2000 lines, use offset/limit"
---

<objective>
Add a chunked-read guard to load.md's TMSL branch so that reading large model.bim files does not trigger the "file content exceeds 10K tokens" error. This is the only command file not covered by Plans 02-03 that reads model.bim without a size guard.

Purpose: TOKEN-01 and TOKEN-02 — the TMSL load path is the last command with an unguarded model.bim read.
Output: load.md with chunked-read instruction in the TMSL Step 2 branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Chunked-read pattern (consistent across all command files in this phase): -->
<!--   "If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines -->
<!--    — read the full file across multiple reads before [processing action]." -->

<!-- Current load.md Step 2 TMSL branch (line ~57-64): -->
<!-- "For TMSL (PBIP_FORMAT=tmsl):" -->
<!-- "Read $PBIP_DIR/model.bim with the Read tool." -->
<!-- "Navigate the JSON structure: model.tables[], model.relationships[]" -->

<!-- audit.md TMSL branch already has: "If model.bim is >2000 lines, use offset/limit parameters." -->
<!-- extract.md TMSL branch already has: "If >2000 lines, use offset/limit parameters to read in chunks." -->
<!-- load.md is the only remaining file missing this guard. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TMSL chunked-read guard to load.md</name>
  <read_first>
    .claude/skills/pbi/commands/load.md (full file — locate the TMSL Step 2 branch, line ~57)
  </read_first>
  <files>.claude/skills/pbi/commands/load.md</files>
  <action>
Read the full load.md. Make exactly one targeted change.

**Change — Step 2 TMSL branch**

Locate the `**For TMSL (`PBIP_FORMAT=tmsl`):**` section in Step 2. Find the line:

```
Read `$PBIP_DIR/model.bim` with the Read tool.
```

Replace with:

```
Read `$PBIP_DIR/model.bim` with the Read tool. If model.bim is >2000 lines, use offset/limit parameters to read it in chunks of 1000 lines — read the full file across multiple reads before navigating the JSON structure.
```

The lines that follow ("Navigate the JSON structure: model.tables[]...", "model.relationships[]...", "Build relationship pairs:") are unchanged.

Write the complete updated file back using the Write tool. This is a single-sentence addition — no other content should change.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL && python -c "
content = open('.claude/skills/pbi/commands/load.md', encoding='utf-8').read()
assert 'offset/limit' in content, 'chunked-read guard missing from load.md'
print('load.md OK — offset/limit guard present')
"</automated>
  </verify>
  <done>
    - load.md contains the phrase "offset/limit" in the TMSL Step 2 branch
    - load.md contains the phrase "chunks of 1000 lines" or equivalent chunking instruction
    - No other content in load.md is changed
  </done>
</task>

</tasks>

<verification>
Confirm load.md has the guard and no unintended changes:

```bash
cd /c/Users/DeveshD/Documents/PBI-SKILL
python -c "
content = open('.claude/skills/pbi/commands/load.md', encoding='utf-8').read()
print('offset/limit present:', 'offset/limit' in content)
# Sanity check key existing phrases still present
print('TMSL section present:', 'PBIP_FORMAT=tmsl' in content)
print('model.bim present:', 'model.bim' in content)
print('model.tables present:', 'model.tables' in content)
"
```

Expected: offset/limit=True, all sanity checks True.
</verification>

<success_criteria>
- load.md TMSL branch contains "offset/limit" in the model.bim read instruction
- load.md is otherwise identical to the pre-plan version (single targeted addition)
- Combined with Plans 02 and 03, every command that reads model.bim (load, edit, comment, error, new) now has a chunked-read guard for the TMSL path
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-safety-utf-8-hardening/06-05-SUMMARY.md` using the summary template.
</output>
