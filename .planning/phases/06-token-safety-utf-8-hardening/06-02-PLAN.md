---
phase: 06-token-safety-utf-8-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .claude/skills/pbi/commands/edit.md
  - .claude/skills/pbi/commands/comment.md
autonomous: true
requirements:
  - UTF8-01
  - TOKEN-01
  - TOKEN-02

must_haves:
  truths:
    - "edit.md contains no grep -rlF calls — measure search uses detect.py search"
    - "comment.md contains no grep -rlF calls — measure search uses detect.py search"
    - "edit.md TMSL branch has a chunked-read note for large model.bim files"
    - "comment.md TMSL branch has a chunked-read note for large model.bim files"
  artifacts:
    - path: ".claude/skills/pbi/commands/edit.md"
      provides: "grep-free measure search + TMSL chunked read guard"
      contains: "detect.py search"
    - path: ".claude/skills/pbi/commands/comment.md"
      provides: "grep-free measure search + TMSL chunked read guard"
      contains: "detect.py search"
  key_links:
    - from: "edit.md Step 2 TMDL branch"
      to: "detect.py search"
      via: "python detect.py search <EntityName> <PBIP_DIR> bash block"
    - from: "comment.md File Write-Back TMDL branch"
      to: "detect.py search"
      via: "python detect.py search <MeasureName> <PBIP_DIR> bash block"
---

<objective>
Replace all `grep -rlF` calls in edit.md and comment.md with `detect.py search` calls, and add a chunked-read guard for TMSL model.bim reads in both files so large models do not trigger token overflow.

Purpose: UTF8-01 (grep replacement) and TOKEN-01/02 (chunked read for TMSL) for the edit and comment command files.
Output: edit.md and comment.md with zero grep calls and a TMSL size guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-token-safety-utf-8-hardening/06-01-SUMMARY.md

<interfaces>
<!-- detect.py search subcommand (already exists since v1.0) -->
<!-- Signature: python detect.py search <name> <pbip_dir> -->
<!-- Prints: one file path per line for each file containing the name string -->
<!-- UTF-8 safe — handles French accents in measure names -->

<!-- CURRENT grep calls to replace in edit.md: -->

edit.md line 40 (TMDL measure resolution):
  BEFORE: grep -rlF "[EntityName]" "$PBIP_DIR/definition/tables/" 2>/dev/null
  AFTER:  python ".claude/skills/pbi/scripts/detect.py" search "[EntityName]" "$PBIP_DIR" 2>/dev/null

edit.md line 44 (zero-results fallback — lists ALL measures):
  BEFORE: grep -r "measure " "$PBIP_DIR/definition/tables/" 2>/dev/null
  AFTER:  python ".claude/skills/pbi/scripts/detect.py" search "measure " "$PBIP_DIR" 2>/dev/null
  NOTE: The zero-results fallback only needs measure names, not file:line detail.
        detect.py search returns filenames that contain "measure " — executor will read
        those files to list measure names. Update the instruction text to match.

<!-- CURRENT grep call to replace in comment.md: -->

comment.md line 24 (TMDL measure file lookup):
  BEFORE: grep -rlF "[MeasureName]" "$PBIP_DIR/definition/tables/" 2>/dev/null
  AFTER:  python ".claude/skills/pbi/scripts/detect.py" search "[MeasureName]" "$PBIP_DIR" 2>/dev/null

<!-- TMSL chunked-read pattern to ADD (per D-02): -->
<!-- Add to the "If PBIP_FORMAT=tmsl" branch in each file's entity resolution/write-back section -->
<!-- Instruction text to insert: -->
<!--   "If model.bim is >2000 lines, use offset/limit parameters on the Read tool to read it -->
<!--    in chunks of 1000 lines. Read the full file across multiple reads before searching." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace grep in edit.md and add TMSL chunked-read guard</name>
  <read_first>
    .claude/skills/pbi/commands/edit.md (full file — identify all grep occurrences before editing)
  </read_first>
  <files>.claude/skills/pbi/commands/edit.md</files>
  <action>
Read the full edit.md. Make exactly three targeted changes:

**Change 1 — Line ~40: TMDL measure search (primary)**

Replace the bash block:
```bash
grep -rlF "[EntityName]" "$PBIP_DIR/definition/tables/" 2>/dev/null
```

With:
```bash
python ".claude/skills/pbi/scripts/detect.py" search "[EntityName]" "$PBIP_DIR" 2>/dev/null
```

The surrounding instruction text says "Run bash:" and handles zero/one/multiple results — keep that text unchanged. Only replace the bash command inside the code block.

**Change 2 — Line ~44: TMDL zero-results fallback**

Replace the bash block in the zero-results branch:
```bash
grep -r "measure " "$PBIP_DIR/definition/tables/" 2>/dev/null
```

With:
```bash
python ".claude/skills/pbi/scripts/detect.py" search "measure " "$PBIP_DIR" 2>/dev/null
```

Also update the instruction text immediately after this bash block. The current text says to "list all measure names" and "Compare requested name to the list". Update it to:
"Read each returned file to list measure names. Compare requested name to the list using common-typo reasoning. Output up to 3 candidates..."
(The rest of the sentence is unchanged.)

**Change 3 — TMSL branch: add chunked-read guard**

In the `**If PBIP_FORMAT=tmsl:**` section of Step 2 (Entity Resolution), find the line:
> Read `$PBIP_DIR/model.bim` (Read tool). Search the JSON `"measures"` arrays for `"name"` matching EntityName.

Replace it with:
> Read `$PBIP_DIR/model.bim` using the Read tool. If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines — read the full file across multiple reads before searching. Search the JSON `"measures"` arrays for `"name"` matching EntityName.

Write the complete updated file back using the Write tool. The file must have zero occurrences of `grep` when done.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL && python -c "
content = open('.claude/skills/pbi/commands/edit.md', encoding='utf-8').read()
assert 'grep' not in content, 'grep still present in edit.md'
assert 'detect.py' in content, 'detect.py not found in edit.md'
assert 'offset/limit' in content, 'chunked-read guard missing from edit.md'
print('edit.md OK')
"</automated>
  </verify>
  <done>
    - edit.md contains zero occurrences of the word `grep`
    - edit.md contains `detect.py` search calls in both the primary and fallback bash blocks
    - edit.md TMSL branch contains the phrase "offset/limit" in the model.bim read instruction
    - All other content in edit.md is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace grep in comment.md and add TMSL chunked-read guard</name>
  <read_first>
    .claude/skills/pbi/commands/comment.md (full file — identify all grep occurrences before editing)
  </read_first>
  <files>.claude/skills/pbi/commands/comment.md</files>
  <action>
Read the full comment.md. Make exactly two targeted changes:

**Change 1 — Line ~24: TMDL measure file lookup (File Write-Back section)**

In the `**If PBIP_FORMAT=tmdl:**` block under "File Write-Back", find:
```bash
grep -rlF "[MeasureName]" "$PBIP_DIR/definition/tables/" 2>/dev/null
```

Replace with:
```bash
python ".claude/skills/pbi/scripts/detect.py" search "[MeasureName]" "$PBIP_DIR" 2>/dev/null
```

The surrounding instruction text says "Run bash:" and handles zero/one/multiple results — keep that text unchanged. Only replace the bash command inside the code block.

**Change 2 — TMSL branch: add chunked-read guard**

In the `**If PBIP_FORMAT=tmsl:**` section under "File Write-Back", find:
> 1. Read `$PBIP_DIR/model.bim` using the Read tool.

Replace with:
> 1. Read `$PBIP_DIR/model.bim` using the Read tool. If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines — read the full file across multiple reads before locating the measure.

Write the complete updated file back using the Write tool. The file must have zero occurrences of `grep` when done.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL && python -c "
content = open('.claude/skills/pbi/commands/comment.md', encoding='utf-8').read()
assert 'grep' not in content, 'grep still present in comment.md'
assert 'detect.py' in content, 'detect.py not found in comment.md'
assert 'offset/limit' in content, 'chunked-read guard missing from comment.md'
print('comment.md OK')
"</automated>
  </verify>
  <done>
    - comment.md contains zero occurrences of the word `grep`
    - comment.md contains `detect.py` search call in the TMDL File Write-Back section
    - comment.md TMSL branch contains the phrase "offset/limit" in the model.bim read instruction
    - All other content in comment.md is unchanged
  </done>
</task>

</tasks>

<verification>
Check both files are grep-free and contain the required patterns:

```bash
cd /c/Users/DeveshD/Documents/PBI-SKILL
python -c "
for fn in ['.claude/skills/pbi/commands/edit.md', '.claude/skills/pbi/commands/comment.md']:
    c = open(fn, encoding='utf-8').read()
    print(fn)
    print('  grep present:', 'grep' in c)
    print('  detect.py present:', 'detect.py' in c)
    print('  offset/limit present:', 'offset/limit' in c)
"
```

Expected: grep=False, detect.py=True, offset/limit=True for both files.
</verification>

<success_criteria>
- edit.md: zero `grep` occurrences, two `detect.py search` calls, one `offset/limit` note in TMSL section
- comment.md: zero `grep` occurrences, one `detect.py search` call, one `offset/limit` note in TMSL section
- No other content changed in either file (only the targeted grep lines and TMSL read instruction)
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-safety-utf-8-hardening/06-02-SUMMARY.md` using the summary template.
</output>
