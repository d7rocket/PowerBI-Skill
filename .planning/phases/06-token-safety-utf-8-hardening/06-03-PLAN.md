---
phase: 06-token-safety-utf-8-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .claude/skills/pbi/commands/error.md
  - .claude/skills/pbi/commands/new.md
autonomous: true
requirements:
  - UTF8-01
  - TOKEN-01
  - TOKEN-02

must_haves:
  truths:
    - "error.md contains no grep -rlF calls — measure search uses detect.py search"
    - "new.md contains no grep -rlF calls — table verification uses detect.py search"
    - "error.md TMSL branch has a chunked-read note for large model.bim files"
    - "new.md TMSL branch has a chunked-read note for large model.bim files"
  artifacts:
    - path: ".claude/skills/pbi/commands/error.md"
      provides: "grep-free measure search + TMSL chunked read guard"
      contains: "detect.py search"
    - path: ".claude/skills/pbi/commands/new.md"
      provides: "grep-free table verification + TMSL chunked read guard"
      contains: "detect.py search"
  key_links:
    - from: "error.md File Fix Preview TMDL branch"
      to: "detect.py search"
      via: "python detect.py search <MeasureName> <PBIP_DIR> bash block"
    - from: "new.md Step 5 TMDL branch"
      to: "detect.py search"
      via: "python detect.py search <table_name> <PBIP_DIR> bash block"
---

<objective>
Replace all `grep -rlF` calls in error.md and new.md with `detect.py search` calls, and add a chunked-read guard for TMSL model.bim reads in both files.

Purpose: UTF8-01 (grep replacement) and TOKEN-01/02 (chunked read for TMSL) for the error and new command files.
Output: error.md and new.md with zero grep calls and a TMSL size guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-token-safety-utf-8-hardening/06-01-SUMMARY.md

<interfaces>
<!-- detect.py search subcommand signature (unchanged from Plan 01) -->
<!-- python detect.py search <name> <pbip_dir> -->
<!-- Prints one file path per line for each file containing the name string -->

<!-- CURRENT grep calls to replace in error.md: -->

error.md line 39 (TMDL measure file lookup in "File Fix Preview and Confirm" section):
  BEFORE: grep -rlF "[MeasureName]" "$PBIP_DIR/definition/tables/" 2>/dev/null
  AFTER:  python ".claude/skills/pbi/scripts/detect.py" search "[MeasureName]" "$PBIP_DIR" 2>/dev/null

<!-- CURRENT grep call to replace in new.md: -->

new.md line 184 (TMDL table verification in Step 5):
  BEFORE: grep -rlF "table " "$PBIP_DIR/definition/tables/" 2>/dev/null
  AFTER:  python ".claude/skills/pbi/scripts/detect.py" search "table " "$PBIP_DIR" 2>/dev/null

  NOTE: This call verifies the target table file exists by listing table names.
  The instruction text currently says "Run bash: ... to verify the target table file exists."
  Keep the instruction text — only replace the bash command. The executor will read the
  returned files to confirm the table exists.

<!-- TMSL chunked-read guard location in error.md: -->
<!-- In "File Fix Preview and Confirm" section, TMSL branch: -->
<!-- "TMSL: Read $PBIP_DIR/model.bim. Locate measure object by name." -->
<!-- Insert chunked-read note before "Locate measure object": -->
<!--   "If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines." -->

<!-- TMSL chunked-read guard location in new.md: -->
<!-- In Step 5, "If PBIP_FORMAT=tmsl:" section: -->
<!-- "1. Read $PBIP_DIR/model.bim using the Read tool." -->
<!-- Append to that line: "If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace grep in error.md and add TMSL chunked-read guard</name>
  <read_first>
    .claude/skills/pbi/commands/error.md (full file — identify the exact grep line and TMSL read instruction)
  </read_first>
  <files>.claude/skills/pbi/commands/error.md</files>
  <action>
Read the full error.md. Make exactly two targeted changes:

**Change 1 — Line ~39: TMDL measure file lookup (File Fix Preview and Confirm section)**

Find the TMDL branch in the "File Fix Preview and Confirm" section:
```
**TMDL:** `grep -rlF "[MeasureName]" "$PBIP_DIR/definition/tables/" 2>/dev/null`
```

Replace the grep command with:
```
**TMDL:** `python ".claude/skills/pbi/scripts/detect.py" search "[MeasureName]" "$PBIP_DIR" 2>/dev/null`
```

Keep the surrounding text (Multiple matches, No match, One match result handling) unchanged.

**Change 2 — TMSL branch: add chunked-read guard**

In the same "File Fix Preview and Confirm" section, find the TMSL branch line:
```
**TMSL:** Read `$PBIP_DIR/model.bim`. Locate measure object by name. If not found: same not-found message.
```

Replace with:
```
**TMSL:** Read `$PBIP_DIR/model.bim` using the Read tool. If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines — read the full file before searching. Locate measure object by name. If not found: same not-found message.
```

Write the complete updated file back using the Write tool. The file must have zero occurrences of `grep -rlF` when done.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL && python -c "
content = open('.claude/skills/pbi/commands/error.md', encoding='utf-8').read()
assert 'grep -rlF' not in content, 'grep -rlF still present in error.md'
assert 'detect.py' in content, 'detect.py not found in error.md'
assert 'offset/limit' in content, 'chunked-read guard missing from error.md'
print('error.md OK')
"</automated>
  </verify>
  <done>
    - error.md contains zero occurrences of `grep -rlF`
    - error.md contains one `detect.py search` call in the TMDL measure lookup
    - error.md TMSL branch contains "offset/limit" in the model.bim read instruction
    - All other content in error.md is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace grep in new.md and add TMSL chunked-read guard</name>
  <read_first>
    .claude/skills/pbi/commands/new.md (full file — identify line 184 grep and the TMSL Step 5 read instruction)
  </read_first>
  <files>.claude/skills/pbi/commands/new.md</files>
  <action>
Read the full new.md. Make exactly two targeted changes:

**Change 1 — Line ~184: TMDL table verification (Step 5 file write)**

In the `**If PBIP_FORMAT=tmdl:**` section of Step 5, find:
```
1. Run bash: `grep -rlF "table " "$PBIP_DIR/definition/tables/" 2>/dev/null` to verify the target table file exists.
```

Replace with:
```
1. Run bash: `python ".claude/skills/pbi/scripts/detect.py" search "table " "$PBIP_DIR" 2>/dev/null` to verify the target table file exists. If no output is returned, the table was not found — output "Table [TableName] not found in $PBIP_DIR/definition/tables/. Check the table name and try again." and stop.
```

Note: The original instruction says to list tables via grep output; detect.py search returns file paths containing "table " (i.e., all table files). The executor can check if any path contains the target table name. Update the note to make this explicit — the rest of the Step 5 TMDL block (Read target .tmdl, scaffold, Write) stays unchanged.

**Change 2 — TMSL branch: add chunked-read guard**

In the `**If PBIP_FORMAT=tmsl:**` section of Step 5, find:
```
1. Read `$PBIP_DIR/model.bim` using the Read tool.
```

Replace with:
```
1. Read `$PBIP_DIR/model.bim` using the Read tool. If model.bim is >2000 lines, use offset/limit parameters to read in chunks of 1000 lines — read the full file before locating the target table's measures array.
```

Write the complete updated file back using the Write tool. The file must have zero occurrences of `grep -rlF` when done.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL && python -c "
content = open('.claude/skills/pbi/commands/new.md', encoding='utf-8').read()
assert 'grep -rlF' not in content, 'grep -rlF still present in new.md'
assert 'detect.py' in content, 'detect.py not found in new.md'
assert 'offset/limit' in content, 'chunked-read guard missing from new.md'
print('new.md OK')
"</automated>
  </verify>
  <done>
    - new.md contains zero occurrences of `grep -rlF`
    - new.md contains one `detect.py search` call in the TMDL table verification
    - new.md TMSL branch contains "offset/limit" in the model.bim read instruction
    - All other content in new.md is unchanged
  </done>
</task>

</tasks>

<verification>
Check both files are grep -rlF free and contain the required patterns:

```bash
cd /c/Users/DeveshD/Documents/PBI-SKILL
python -c "
for fn in ['.claude/skills/pbi/commands/error.md', '.claude/skills/pbi/commands/new.md']:
    c = open(fn, encoding='utf-8').read()
    print(fn)
    print('  grep -rlF present:', 'grep -rlF' in c)
    print('  detect.py present:', 'detect.py' in c)
    print('  offset/limit present:', 'offset/limit' in c)
"
```

Expected: grep -rlF=False, detect.py=True, offset/limit=True for both files.
</verification>

<success_criteria>
- error.md: zero `grep -rlF` occurrences, one `detect.py search` call, one `offset/limit` note in TMSL section
- new.md: zero `grep -rlF` occurrences, one `detect.py search` call, one `offset/limit` note in TMSL section
- No other content changed in either file (only the targeted grep lines and TMSL read instructions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-safety-utf-8-hardening/06-03-SUMMARY.md` using the summary template.
</output>
