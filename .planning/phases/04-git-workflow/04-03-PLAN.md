---
phase: 04-git-workflow
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - .claude/skills/pbi-commit/SKILL.md
autonomous: true
requirements:
  - GIT-04
  - GIT-05
  - GIT-07
  - GIT-08

must_haves:
  truths:
    - "Analyst can run /pbi:commit to stage .SemanticModel/ changes and create a local git commit with a conventional commits message naming actual measures and tables"
    - "Commit message subject line names the primary model change; body has one bullet per changed item"
    - "If no git repo exists, /pbi:commit runs git init, creates .gitignore with PBIP noise entries, and creates initial commit"
    - "No git push occurs under any circumstances — ever"
    - "If no PBIP project detected, skill outputs a clear stop message and does not proceed"
  artifacts:
    - path: ".claude/skills/pbi-commit/SKILL.md"
      provides: "/pbi:commit command implementation"
      exports: ["git repo check", "git init flow", "diff parse", "conventional commit generation", "git add + commit"]
  key_links:
    - from: "diff parse (same rules as pbi-diff)"
      to: "conventional commit prefix inference"
      via: "change type table in SKILL.md"
      pattern: "feat:|fix:|chore:"
    - from: "git add '.SemanticModel/'"
      to: "git commit -m"
      via: "staged PBIP files only"
      pattern: "git add.*SemanticModel"
---

<objective>
Create the `/pbi:commit` skill that stages PBIP-scoped changes, generates a conventional commits message that names the actual model changes, and handles the git init flow for first-time use. Push to remote is explicitly never triggered.

Purpose: Give analysts one command to save a meaningful snapshot of model changes with a business-language commit message, without needing to know git conventions.
Output: `.claude/skills/pbi-commit/SKILL.md`
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-git-workflow/04-CONTEXT.md
@.planning/phases/04-git-workflow/04-RESEARCH.md
@.planning/phases/04-git-workflow/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor needs -->

Frontmatter pattern:
```yaml
---
name: pbi-commit
description: Stage PBIP model changes and create a local git commit with an auto-generated business-language message. Use when an analyst wants to commit changes or save a snapshot of the model.
disable-model-invocation: true
model: sonnet
allowed-tools: Read, Write, Bash
---
```

Repo existence check (from RESEARCH.md):
```bash
GIT_INSIDE=$(git rev-parse --is-inside-work-tree 2>/dev/null && echo "GIT=yes" || echo "GIT=no")
HAS_COMMITS=$(git rev-parse HEAD 2>/dev/null && echo "HAS_COMMITS=yes" || echo "HAS_COMMITS=no")
```

Git init flow (GIT-08, locked decision from CONTEXT.md):
1. `git init`
2. Write `.gitignore` with Write tool (content below)
3. `git add '.SemanticModel/' '.gitignore' && git commit -m "chore: initial PBIP model commit"`

.gitignore content template (locked in RESEARCH.md):
```
# Power BI noise files
*.abf
localSettings.json
.pbi-context.md
SecurityBindings
*.pbids
cache/
```

Stage PBIP files only (never git add . or -A):
```bash
git add '.SemanticModel/' 2>/dev/null
```

Commit with multi-line message:
```bash
git commit -m "feat: add [Revenue YTD] measure to Sales

- add [Revenue YTD] to Sales
- modify [Total Cost] expression in Products" 2>/dev/null
```

Conventional commit prefix inference table (locked in CONTEXT.md/RESEARCH.md):
| Change detected | Prefix |
|----------------|--------|
| Any measure or table ADDED | feat: |
| Any measure REMOVED | fix: |
| Only expression or description changes | chore: |
| Only formatString / displayFolder changes | chore: |
| Relationship added | feat: |
| Relationship removed | fix: |
| Mixed adds and changes | feat: (most significant wins) |

Subject line pattern: "[prefix] [primary verb] [primary item] in [table/model]"
- e.g. `feat: add [Revenue YTD] measure to Sales`
- e.g. `chore: update display folder for 3 measures in Products`
- e.g. `fix: remove bidirectional filter on Sales → Date relationship`

Body: one bullet per changed item, e.g.:
```
- add [Revenue YTD] to Sales
- modify [Total Cost] expression in Products
```

TMDL diff parsing rules and TMSL parsing rules: identical to Plan 02 (pbi-diff). Same logic, applied here for message generation. See Plan 02 interfaces block for full rules.

CRITICAL: no `git push` in this skill under ANY circumstances. Not optional, not conditional.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create pbi-commit/SKILL.md</name>
  <files>.claude/skills/pbi-commit/SKILL.md</files>
  <action>
    Create `.claude/skills/pbi-commit/SKILL.md` with the following exact structure.

    **Frontmatter:**
    ```yaml
    ---
    name: pbi-commit
    description: Stage PBIP model changes and create a local git commit with an auto-generated business-language message. Use when an analyst wants to commit changes or save a snapshot of the model.
    disable-model-invocation: true
    model: sonnet
    allowed-tools: Read, Write, Bash
    ---
    ```

    **Startup bash blocks (in this exact order):**

    1. PBIP Detection block (identical to all other skills — copy exactly from any existing skill)
    2. Git state check block:
       ```
       !`GIT_INSIDE=$(git rev-parse --is-inside-work-tree 2>/dev/null && echo "GIT=yes" || echo "GIT=no"); HAS_COMMITS=$(git rev-parse HEAD 2>/dev/null && echo "HAS_COMMITS=yes" || echo "HAS_COMMITS=no"); echo "$GIT_INSIDE $HAS_COMMITS"`
       ```
    3. Session context block (identical to all other skills)

    **Step 0 — Check PBIP detection:**
    - If PBIP_MODE=paste: output `No PBIP project found. Run /pbi:commit from a directory containing .SemanticModel/.` and stop.
    - If PBIP_MODE=file AND GIT=no: proceed to Step 1a (git init flow).
    - If PBIP_MODE=file AND GIT=yes AND HAS_COMMITS=no: proceed to Step 1b (initial commit on existing empty repo).
    - If PBIP_MODE=file AND GIT=yes AND HAS_COMMITS=yes: proceed to Step 2 (normal commit flow).

    **Step 1a — Git init flow (GIT-08: no git repo exists):**
    1. Run `git init` as a bash block. Capture output.
    2. Write `.gitignore` using the Write tool with this exact content:
       ```
       # Power BI noise files
       *.abf
       localSettings.json
       .pbi-context.md
       SecurityBindings
       *.pbids
       cache/
       ```
    3. Run `git add '.SemanticModel/' '.gitignore' && git commit -m "chore: initial PBIP model commit"` as a bash block.
    4. Output to analyst:
       > Git repo initialised. Initial commit created: chore: initial PBIP model commit
       > To push to a remote: git remote add origin <url> && git push -u origin main
    5. Proceed to context update (Step 5). Do NOT run Step 2-4 after init.

    **Step 1b — Initial commit on empty repo (repo exists but no commits):**
    1. Treat all .SemanticModel/ files as new.
    2. Run `git add '.SemanticModel/' && git commit -m "chore: initial PBIP model commit"` as a bash block.
    3. Output to analyst:
       > Initial commit created: chore: initial PBIP model commit
    4. Proceed to context update (Step 5). Do NOT run Step 2-4 after initial commit.

    **Step 2 — Get diff for message generation:**
    Run the PBIP-scoped diff command as a bash block (same as pbi-diff Step 2 — same TMDL/TMSL branching):
    - TMDL: `git diff HEAD -- '.SemanticModel/definition/tables/' '.SemanticModel/definition/relationships.tmdl' 2>/dev/null`
    - TMSL: `git diff HEAD -- '.SemanticModel/model.bim' 2>/dev/null`

    If the diff output is empty (nothing to commit): output `No changes to commit in .SemanticModel/.` and stop (do not attempt git add or commit).

    **Step 3 — Parse diff and generate commit message:**
    Apply the same TMDL and TMSL parsing rules as pbi-diff (identical rules — see interfaces). Then:

    1. Apply the conventional commit prefix inference table to determine prefix (feat/fix/chore).
    2. Build subject line: `[prefix]: [primary verb] [primary item] in [table/model]` — max 72 chars.
    3. Build body: one bullet per changed item (`- add [Name] to Table`, `- modify [Name] expression in Table`, `- remove [Name] from Table`).
    4. Full commit message = subject + blank line + body (omit body if only one change).

    Show the analyst the planned commit message before executing:
    ```
    **Committing with message:**
    [subject line]

    [body if present]
    ```

    **Step 4 — Stage and commit:**
    Run as a single bash block:
    ```bash
    git add '.SemanticModel/' 2>/dev/null && git commit -m "[full message]" 2>/dev/null && echo "COMMIT=ok" || echo "COMMIT=fail"
    ```

    - COMMIT=ok: output `Committed. Run: git push` (reminder that push is always manual — never automatic).
    - COMMIT=fail: output `Commit failed — check git status. File changes are intact.`

    CRITICAL GUARD: There is absolutely no `git push` in any bash block or instruction in this skill. The reminder line `Run: git push` is output text only — not a bash command.

    **Step 5 — Context update:**
    Read then Write `.pbi-context.md`:
    - Update `## Last Command`: Command = `/pbi:commit`, Outcome = commit subject line.
    - Append row to `## Command History`.
    - Keep to 20 rows max.
    - Do not modify `## Analyst-Reported Failures`.
  </action>
  <verify>
    <automated>test -f /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi-commit/SKILL.md && grep -q "pbi-commit" /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi-commit/SKILL.md && ! grep -q "git push" /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi-commit/SKILL.md && echo "NO_PUSH_OK" && grep -q "git add '.SemanticModel/'" /c/Users/DeveshD/Documents/PBI-SKILL/.claude/skills/pbi-commit/SKILL.md && echo "SCOPED_ADD_OK"</automated>
  </verify>
  <done>
    - .claude/skills/pbi-commit/SKILL.md exists with correct frontmatter
    - Contains PBIP detection, git state check, session context bash blocks
    - Step 1a is the git init flow: git init + Write .gitignore + initial commit
    - Step 2 uses scoped git diff (not unscoped)
    - Step 4 uses `git add '.SemanticModel/'` (not git add . or git add -A)
    - Zero occurrences of `git push` as a bash command
    - Conventional commit prefix inference table present
    - Empty diff case handled (outputs "No changes to commit" and stops)
  </done>
</task>

</tasks>

<verification>
Manual verification after skill creation:
1. `grep "git push" .claude/skills/pbi-commit/SKILL.md` — must return 0 matches (GIT-07)
2. `grep "git add" .claude/skills/pbi-commit/SKILL.md` — must show `.SemanticModel/` scoping, never `git add .`
3. Manual: run `/pbi:commit` from `tests/fixtures/pbip-no-repo/`; verify git init runs, .gitignore written, initial commit created
4. Manual: run `/pbi:commit` from `tests/fixtures/pbip-tmdl/` after modifying Sales.tmdl; verify commit message names the measure and table
</verification>

<success_criteria>
- Skill file exists with correct frontmatter (model: sonnet, allowed-tools: Read Write Bash)
- GIT-08: git init flow creates repo, writes .gitignore, creates initial commit
- GIT-04/05: normal flow stages .SemanticModel/ and commits with conventional commit message naming actual changes
- GIT-07: zero git push commands in the skill file
- Empty diff case handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/04-git-workflow/04-03-SUMMARY.md`
</output>
