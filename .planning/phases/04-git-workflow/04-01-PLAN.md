---
phase: 04-git-workflow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/pbip-tmdl/.git/
  - tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition.pbism
  - tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/relationships.tmdl
  - tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Sales.tmdl
  - tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Date.tmdl
  - tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Products.tmdl
  - tests/fixtures/pbip-no-repo/.SemanticModel/definition.pbism
  - tests/fixtures/pbip-no-repo/.SemanticModel/definition/relationships.tmdl
  - tests/fixtures/pbip-no-repo/.SemanticModel/definition/tables/Sales.tmdl
autonomous: true
requirements:
  - GIT-01
  - GIT-03
  - GIT-04
  - GIT-08

must_haves:
  truths:
    - "git diff HEAD scoped to .SemanticModel/ returns diff output against a known baseline in pbip-tmdl fixture"
    - "pbip-tmdl-no-gitignore fixture has no .gitignore — triggers GIT-03 auto-fix test path"
    - "pbip-no-repo fixture has PBIP files but no .git/ folder — triggers GIT-08 init flow test path"
  artifacts:
    - path: "tests/fixtures/pbip-tmdl/.git/"
      provides: "git history baseline so git diff HEAD works in TMDL fixture"
    - path: "tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition.pbism"
      provides: "TMDL fixture variant with no .gitignore for GIT-03 testing"
    - path: "tests/fixtures/pbip-no-repo/.SemanticModel/definition.pbism"
      provides: "PBIP files with no .git/ for GIT-08 testing"
  key_links:
    - from: "tests/fixtures/pbip-tmdl/"
      to: "git diff HEAD"
      via: "initial commit baseline"
      pattern: "git log --oneline"
---

<objective>
Create the three Wave 0 test fixtures that validation scenarios for GIT-01, GIT-03, GIT-04, and GIT-08 depend on. Without these, Plans 02 and 03 have no way to verify their behavior.

Purpose: Establish the test infrastructure that makes all subsequent git workflow verification possible.
Output: (1) `pbip-tmdl` fixture with a git repo and initial commit baseline, (2) `pbip-tmdl-no-gitignore` copy of the TMDL fixture with no `.gitignore`, (3) `pbip-no-repo` directory with PBIP files and no `.git/` folder.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-git-workflow/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialise git repo in pbip-tmdl fixture and create baseline commit</name>
  <files>tests/fixtures/pbip-tmdl/.git/</files>
  <action>
    Run from the project root. The goal is a git repo in `tests/fixtures/pbip-tmdl/` with an initial commit that captures the current `.SemanticModel/` state as the baseline. Subsequent diff tests will modify one file and run `git diff HEAD` to detect the change.

    Steps:
    1. Check if `tests/fixtures/pbip-tmdl/.git/` already exists. If it does, skip git init.
    2. If no repo: run `git init` inside `tests/fixtures/pbip-tmdl/`.
    3. Create `tests/fixtures/pbip-tmdl/.gitignore` with these exact entries:
       ```
       # Power BI noise files
       *.abf
       localSettings.json
       .pbi-context.md
       SecurityBindings
       *.pbids
       cache/
       ```
    4. Stage and commit: `git add .SemanticModel/ .gitignore && git commit -m "chore: initial PBIP model commit"` inside `tests/fixtures/pbip-tmdl/`.
    5. Verify with `git log --oneline` — should show exactly one commit.

    IMPORTANT: This is a nested git repo inside the project root's non-git directory. Run all git commands with `cd tests/fixtures/pbip-tmdl && git ...` pattern so they target the fixture's repo, not any parent repo.
  </action>
  <verify>
    <automated>cd /c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl && git log --oneline 2>/dev/null | grep "initial PBIP model commit"</automated>
  </verify>
  <done>git log inside pbip-tmdl shows one commit with message "chore: initial PBIP model commit"; .gitignore exists in the fixture root</done>
</task>

<task type="auto">
  <name>Task 2: Create pbip-tmdl-no-gitignore fixture (TMDL copy without .gitignore)</name>
  <files>
    tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition.pbism
    tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/relationships.tmdl
    tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Sales.tmdl
    tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Date.tmdl
    tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition/tables/Products.tmdl
  </files>
  <action>
    Copy the entire `.SemanticModel/` tree from `tests/fixtures/pbip-tmdl/` into a new directory `tests/fixtures/pbip-tmdl-no-gitignore/`. Do NOT copy `.git/` or `.gitignore` from the source fixture — this variant must have no `.gitignore` to trigger the GIT-03 auto-fix code path.

    Also initialise a git repo inside this fixture with an initial commit (same as Task 1 but without `.gitignore`):
    - `git init` inside `tests/fixtures/pbip-tmdl-no-gitignore/`
    - `git add .SemanticModel/ && git commit -m "chore: initial PBIP model commit"`

    This gives `/pbi:diff` a valid git repo to query, but without `.gitignore` entries — confirming the auto-fix adds them.

    Use Write tool to create each file, reading the source files first. The `.SemanticModel/definition.pbism` content must be identical to the TMDL source (format detection depends on it).
  </action>
  <verify>
    <automated>ls /c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/definition.pbism && ! test -f /c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-tmdl-no-gitignore/.gitignore && echo "OK: no gitignore"</automated>
  </verify>
  <done>pbip-tmdl-no-gitignore/ exists with full .SemanticModel/ tree, a git repo with baseline commit, and no .gitignore file</done>
</task>

<task type="auto">
  <name>Task 3: Create pbip-no-repo fixture (PBIP files with no git repo)</name>
  <files>
    tests/fixtures/pbip-no-repo/.SemanticModel/definition.pbism
    tests/fixtures/pbip-no-repo/.SemanticModel/definition/relationships.tmdl
    tests/fixtures/pbip-no-repo/.SemanticModel/definition/tables/Sales.tmdl
  </files>
  <action>
    Create a minimal TMDL PBIP fixture at `tests/fixtures/pbip-no-repo/` — just enough to trigger the GIT-08 init flow. No git init here; this directory must have NO `.git/` folder. The `/pbi:commit` skill will initialise one during the test.

    Minimum required structure:
    - `tests/fixtures/pbip-no-repo/.SemanticModel/definition.pbism` — copy content from `tests/fixtures/pbip-tmdl/.SemanticModel/definition.pbism` (ensures TMDL format detection works)
    - `tests/fixtures/pbip-no-repo/.SemanticModel/definition/relationships.tmdl` — copy from TMDL source
    - `tests/fixtures/pbip-no-repo/.SemanticModel/definition/tables/Sales.tmdl` — copy Sales.tmdl from TMDL source (one table is sufficient for init testing)

    Do NOT run `git init` in this directory. The entire purpose is to have a PBIP project with no git history.

    Use Write tool to create each file after reading the source files.
  </action>
  <verify>
    <automated>ls /c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-no-repo/.SemanticModel/definition.pbism && ! test -d /c/Users/DeveshD/Documents/PBI-SKILL/tests/fixtures/pbip-no-repo/.git && echo "OK: no git repo"</automated>
  </verify>
  <done>pbip-no-repo/ exists with .SemanticModel/ structure and definition.pbism; no .git/ directory present</done>
</task>

</tasks>

<verification>
All three fixtures verified:
1. `cd tests/fixtures/pbip-tmdl && git log --oneline` — shows baseline commit
2. `ls tests/fixtures/pbip-tmdl-no-gitignore/.SemanticModel/` — shows model files, no .gitignore
3. `ls tests/fixtures/pbip-no-repo/.SemanticModel/` — shows model files, no .git/
</verification>

<success_criteria>
- `git diff HEAD` in pbip-tmdl returns empty output (no changes since baseline commit)
- pbip-tmdl-no-gitignore has git repo but no .gitignore
- pbip-no-repo has PBIP files with no git repo at all
- All three Wave 0 requirements from VALIDATION.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-git-workflow/04-01-SUMMARY.md`
</output>
