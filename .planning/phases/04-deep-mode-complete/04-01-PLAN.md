---
phase: 04-deep-mode-complete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi/commands/deep.md
autonomous: true
requirements:
  - PHASE-01
  - VERF-01
  - VERF-02
  - VERF-03

must_haves:
  truths:
    - "User entering /pbi deep sees a model review phase (Phase B) that surfaces M:M relationships, missing date tables, and bidirectional filters before any DAX is generated"
    - "User at a phase gate must type 'continue' — the gate re-outputs itself if any other input is given (hard gate, no soft interpretation)"
    - "User at the start of each phase (B, C, D) sees an explicit context summary block with business question, model, and existing measures drawn from .pbi-context.md"
    - "User at session end sees the final verification gate restate the verbatim business question and list measures created, blocking close until answered 'yes'"
    - "User who types 'ok' or 'sounds good' at a phase gate does not advance — gate holds and re-outputs"
  artifacts:
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Four-phase deep mode workflow with hard gates and context re-injection"
      contains: "Phase A, Phase B, Phase C, Phase D"
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Hard gate instruction blocks with three-branch logic"
      contains: "re-output the gate"
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Context summary block at each phase start"
      contains: "Current session context"
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Model health review using audit.md rules conversationally"
      contains: "Model Review"
  key_links:
    - from: "Phase A (Context Intake)"
      to: "Gate A→B"
      via: "hard gate after intake completes"
      pattern: "Gate: Intake to Model Review"
    - from: "Gate A→B"
      to: "Phase B (Model Review)"
      via: "continue token"
      pattern: "proceed to Phase B"
    - from: "Phase B (Model Review)"
      to: "Gate B→C"
      via: "hard gate after model review completes"
      pattern: "Gate: Model Review to DAX"
    - from: "Phase D"
      to: "close confirmation"
      via: "business question verification + yes response"
      pattern: "confirm to close"
---

<objective>
Rewrite `commands/deep.md` from a two-step intake+gate stub into a complete four-phase structured workflow.

Purpose: Deep mode currently gathers context and opens an unstructured DAX session. Phase 4 makes it a disciplined workflow with model health analysis before any DAX is written, hard phase boundaries that cannot be bypassed, context carried forward to prevent drift, and a hardened final gate that verifies output answers the business question.

Output: A fully rewritten `commands/deep.md` implementing the Phase A → B → C → D structure with hard gates, context re-injection, model review, and hardened final verification.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deep-mode-complete/04-RESEARCH.md
@.claude/skills/pbi/commands/deep.md
</context>

<interfaces>
<!-- Key patterns from existing deep.md that must be preserved or updated -->

From .claude/skills/pbi/commands/deep.md (current):

Step 0 — reads .pbi-context.md for existing sections, skips intake questions if already answered
Step 1 — gathers Business Question, Data Model State, Existing Measures one at a time
Step 2 — writes .pbi-context.md (Business Question, Existing Measures, Last Command, Command History)
Step 3 — outputs context summary, opens to "What would you like to work on first?"
Step 4 — Measures Gate triggered by completion phrases ("done", "finished", etc.)

Anti-patterns note in current deep.md:
"NEVER impose phase gates in Phase 1 — that's Phase 3 scope" — this is a STALE note from original
Phase 1 planning. Remove it and replace with current anti-patterns in the rewrite.

From .planning/phases/04-deep-mode-complete/04-RESEARCH.md — prescribed patterns:

Hard Gate:
  Output: "> **--- Gate: [Name] ---**"
  Tokens: "continue" (advance) / "cancel" (stop)
  Third branch: anything else → re-output gate. Do NOT advance.
  Mid-session gates use "continue"/"cancel"
  Terminal close gate preserves "confirm"/"cancel" (existing Step 4 pattern)

Context Re-injection Block (at start of Phase B, C, D):
  Read .pbi-context.md, output:
  "> **Current session context:**"
  "- Business question: [## Business Question or '(not set)']"
  "- Model: [## Model Context first 2 lines or '(not set)']"
  "- Existing measures: [## Existing Measures or '(none noted)']"
  If any "(not set)": pause and collect it before continuing.

Model Health Review (Phase B) — conversational only, NO .SemanticModel file reads:
  CRITICAL: bidirectional relationships, many-to-many (M:M)
  WARN: no date/calendar table, isolated fact table
  Output format: CRITICAL → WARN → INFO
  If no issues: "No structural issues detected in your described model."
  If description too sparse: "Model description is brief — no specific issues detected. If you
  have known relationship concerns, describe them and I'll flag risks."

Phase D Final Verification (hardened):
  Re-outputs verbatim business question from ## Business Question
  Lists all /pbi new rows from ## Command History
  "yes" or clear affirmative (yep, yeah, correct, they do) → output close prompt (confirm/cancel)
  "no" or negative → "What's missing? Continue with /pbi new." Resume Phase C.
  Anything else → re-output the question. Do NOT advance.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite deep.md — Phase A + Phase B + Gate A→B</name>
  <files>.claude/skills/pbi/commands/deep.md</files>
  <action>
Rewrite `commands/deep.md` with the new four-phase structure. In this task, implement Phase A and Phase B with the Gate A→B between them.

**Phase structure to implement in full file rewrite:**

```
Phase A — Context Intake   (preserves current Steps 0–2 logic, now named Phase A)
Gate A→B                   (new hard gate — "continue"/"cancel" tokens)
Phase B — Model Review     (new — PHASE-01)
Gate B→C                   (new hard gate — "continue"/"cancel" tokens)
Phase C — DAX Development  (preserves current Step 3 "What would you like to work on" state)
Phase D — Final Verification (hardened Step 4 — VERF-02)
Anti-Patterns              (updated — remove stale Phase 1 deferral note)
```

**Phase A — Context Intake** (keep current Step 0 + Step 1 + Step 2 logic, renamed):
- Step 0: Check existing context in .pbi-context.md, display what exists, skip intake for answered sections
- Step 1: Gather Business Question, Data Model State, Existing Measures one at a time (wait between each)
- Step 2: Write .pbi-context.md — Business Question, Existing Measures, Last Command, Command History
- Do NOT write Model Context (managed by /pbi load)

**Gate A→B** — immediately after Phase A write completes:

Output exactly:
> **--- Gate: Model Review ---**
> Context gathered. Next: I will review your described model for structural issues before any DAX is written.
>
> Type **continue** to proceed, or **cancel** to stop the session.

Wait for response.
- "continue" (case-insensitive): proceed to Phase B.
- "cancel": output "Session paused. Use /pbi deep to restart." and stop.
- Any other response: re-output the gate prompt above. Do NOT advance.

**Phase B — Model Review** (new):
1. Output context summary block (VERF-03):
   Read .pbi-context.md. Output:
   > **Current session context:**
   > - Business question: [## Business Question content, or "(not set)"]
   > - Model: [## Model Context first 2 lines, or "(not set)"]
   > - Existing measures: [## Existing Measures content, or "(none noted)"]

   If any field is "(not set)": pause and collect it before continuing (re-run appropriate intake question from Phase A Step 1).

2. Analyze described model from ## Model Context for health issues (conversational only — do NOT read .SemanticModel files):
   - CRITICAL: bidirectional relationship — any mention of "both directions", "both ways", "bi-directional", or filter going both ways
   - CRITICAL: many-to-many (M:M) — any description where both sides of a relationship are "many"
   - WARN: no date/calendar table — no table described as a date, calendar, or time dimension
   - WARN: isolated fact table — a table with "Sales", "Orders", "Transactions" in its name and no described relationship

3. Output findings:
   > **Model Review:**
   > [CRITICAL findings first, then WARN, then INFO]
   > [If no issues: "No structural issues detected in your described model."]
   > [If description too sparse to evaluate: "Model description is brief — no specific issues detected. If you have known relationship concerns, describe them and I'll flag risks."]
   > [If issues found]: "These findings won't block DAX work, but addressing them now will improve measure reliability."

4. Proceed to Gate B→C.

**Gate B→C** — immediately after Phase B findings are output:

Output exactly:
> **--- Gate: DAX Development ---**
> Model review complete. Next: DAX development phase where we build measures for your business question.
>
> Type **continue** to proceed, or **cancel** to stop the session.

Wait for response.
- "continue" (case-insensitive): proceed to Phase C.
- "cancel": output "Session paused. Use /pbi deep to restart." and stop.
- Any other response: re-output the gate prompt above. Do NOT advance.

**Phase C — DAX Development** (preserves existing Step 3 behavior):
1. Output context summary block (VERF-03):
   Read .pbi-context.md. Output the same context summary as Phase B (business question, model, existing measures).
   If any field is "(not set)": pause and collect before continuing.

2. Output:
   > **DAX Development phase:**
   > Context saved to .pbi-context.md. All /pbi commands will use this context going forward.
   >
   > What would you like to work on first?
   > When you've finished generating measures, say "done" to review your session before closing.

**Phase D — Final Verification** (hardened from current Step 4):
Activates when analyst signals completion.
Trigger phrases (case-insensitive): "done", "that's all the measures", "finished", "I think we're good", "complete", "all done", "that's it"
Do NOT trigger after each individual /pbi new call. Wait for explicit signal.

1. Output context summary block (VERF-03):
   Read .pbi-context.md. Output the same context summary block.

2. Collect ## Business Question (verbatim) and all /pbi new rows from ## Command History.

3. Output:
   > **Final verification:**
   >
   > **Business question:** [verbatim content of ## Business Question]
   >
   > **Measures created this session:**
   > [For each /pbi new row in Command History: "- [Measure Name]"]
   > (If no /pbi new rows: "- No measures generated via /pbi new in this session.")
   >
   > Do these measures answer the stated business question? (yes / no)

4. Wait for response:
   - "yes" (or clear affirmative — "yep", "yeah", "correct", "they do"): output close prompt:
     > All measures complete — confirm to close the deep mode session. (confirm / cancel)
   - "no" or any negative: output "What's missing? Continue with /pbi new." Resume Phase C state.
   - Anything else: re-output the question above. Do NOT advance.

5. Wait for confirm/cancel response:
   - "confirm": Output "Deep mode session closed. Use /pbi diff or /pbi commit to review and save your changes."
   - "cancel" (or anything else): Output "Session kept open. Continue with /pbi new or other commands." Resume Phase C state.

**Anti-Patterns section** (replace current content — remove stale Phase 1 deferral note):
- NEVER enter deep mode unless the user explicitly typed `/pbi deep` — no automatic upgrade
- NEVER ask all 3 intake questions at once — ask one, wait, ask next
- NEVER re-ask a question if the answer is already in `.pbi-context.md`
- NEVER advance past a phase gate on vague input ("ok", "sounds good", "yes") — the gate token must be "continue" (case-insensitive). Re-output the gate.
- NEVER run model review against .SemanticModel files — Phase B operates on described context only. For file-level audit, direct the user to `/pbi audit`.
- NEVER generate DAX before Gate A→B is confirmed — model review must complete first
- NEVER trigger the measures gate (Phase D) after each individual /pbi new call — it fires only on analyst completion signal
- NEVER advance past the final gate if the business question check returns "no" — offer to continue generating
- NEVER impose gates on individual /pbi new calls within Phase C — gates are at phase boundaries only
  </action>
  <verify>
    <automated>MISSING — Wave 0 acceptance scenarios must be created in Plan 02 before manual verification. Smoke check: open .claude/skills/pbi/commands/deep.md and confirm it contains all four phase headings: "Phase A", "Phase B", "Phase C", "Phase D", plus "Gate A→B", "Gate B→C", and "re-output the gate".</automated>
  </verify>
  <done>
    deep.md contains all four phase sections. Hard gate instruction blocks include three-branch logic with explicit "re-output the gate" instruction. Context summary block appears at Phase B, C, and D starts. Model health review is scoped to described context only (no .SemanticModel reads). Final verification gate outputs verbatim business question. Stale Phase 1 anti-pattern note removed.
  </done>
</task>

</tasks>

<verification>
Open `.claude/skills/pbi/commands/deep.md` and verify:
1. File contains "Phase A", "Phase B", "Phase C", "Phase D" section headings
2. File contains "Gate A→B" and "Gate B→C" hard gate blocks
3. Each gate block contains "re-output the gate" instruction (not just "if not continue, stop")
4. Context summary block appears at start of Phase B, Phase C, and Phase D
5. Phase B model review contains CRITICAL/WARN checks for bidirectional, M:M, missing date table, isolated fact table
6. Phase B contains "Do NOT read .SemanticModel files" or equivalent constraint
7. Phase D outputs verbatim business question from .pbi-context.md
8. Anti-patterns section does NOT contain "NEVER impose phase gates in Phase 1 — that's Phase 3 scope"
</verification>

<success_criteria>
deep.md is a complete four-phase workflow where:
1. No DAX is generated before Phase B model review completes and Gate A→B is confirmed
2. Typing "ok" or "sounds good" at any gate does not advance — gate re-outputs
3. Business question, model, and existing measures are restated at each phase start (B, C, D)
4. Final verification gate states the verbatim business question before asking for confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/04-deep-mode-complete/04-01-SUMMARY.md`
</output>
