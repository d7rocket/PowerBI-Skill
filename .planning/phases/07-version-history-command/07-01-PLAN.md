---
phase: 07-version-history-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi/shared/CHANGELOG.md
  - .claude/skills/pbi/commands/version.md
autonomous: true
requirements: [HIST-01, HIST-02]
must_haves:
  truths:
    - "Running /pbi version displays the current version and full changelog without any network call"
    - "All shipped versions v1.0 through v4.3.0 appear in the changelog with meaningful release notes"
    - "The command file reads CHANGELOG.md from the shared/ directory using Python/UTF-8"
  artifacts:
    - path: ".claude/skills/pbi/shared/CHANGELOG.md"
      provides: "Full version history content for all shipped releases"
      contains: "v4.3.0"
    - path: ".claude/skills/pbi/commands/version.md"
      provides: "version subcommand instructions"
      contains: "CHANGELOG.md"
  key_links:
    - from: ".claude/skills/pbi/commands/version.md"
      to: ".claude/skills/pbi/shared/CHANGELOG.md"
      via: "Python read with UTF-8 encoding"
      pattern: "CHANGELOG"
---

<objective>
Create the CHANGELOG.md file and the version.md command file for the `/pbi version` subcommand.

Purpose: Give analysts a way to see the full skill version history without leaving their Claude Code session. The changelog is bundled in the distribution so it works offline.
Output: `.claude/skills/pbi/shared/CHANGELOG.md` with all v1.0-v4.3.0 entries, and `.claude/skills/pbi/commands/version.md` that reads and displays it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-version-history-command/07-CONTEXT.md

<interfaces>
From .claude/skills/pbi/SKILL.md (routing section, lines 88-91):
For sonnet subcommands (explain, format, optimise, comment, error, new, edit, comment-batch, audit, deep, extract, docs, help):
1. Determine the skill directory — find where this SKILL.md file is located (typically .claude/skills/pbi/)
2. Use the Read tool to load the command file at that path
3. Execute the loaded instructions directly in the current context

From .claude/skills/pbi/commands/help.md (pattern to follow):
- Step 1: Run a bash block to gather data, parse output
- Step 2: Output formatted content directly
- Ends with: "Stop. Do not output anything else."
- Includes Anti-Patterns section

From .claude/skills/pbi/scripts/detect.py (version-check subcommand, lines 178-195):
Prints: LOCAL=version_number
Usage: python ".claude/skills/pbi/scripts/detect.py" version-check "$SKILL_FILE"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md in shared/</name>
  <files>.claude/skills/pbi/shared/CHANGELOG.md</files>
  <read_first>
    - .claude/skills/pbi/SKILL.md (lines 1-14 — current version 4.3.0 and metadata)
    - .claude/skills/pbi/shared/ directory (ls to confirm it exists before writing)
  </read_first>
  <action>
Create `.claude/skills/pbi/shared/CHANGELOG.md` using the Keep a Changelog format. All entries are factual — derived from git log and skill documentation.

The file must start with:
```
# Changelog

All notable changes to the PBI skill are documented here.
Format: [Keep a Changelog](https://keepachangelog.com)
```

Then include one `## [version] — date` section per released version, from newest to oldest:

**[4.3.0] — 2026-03-23** (Changed section):
- Python-first UTF-8 file operations: all measure search now uses `detect.py search` instead of `grep -rlF` in edit.md, comment.md, error.md, and new.md
- `format.md` HTML response parsing migrated to `detect.py html-parse` Python subcommand
- `help.md` version check migrated to `detect.py version-check` Python subcommand
- `detect.py` expanded with three new subcommands: `html-parse`, `version-check`, `gitignore-check`
- TMSL large-file guard added: commands reading model.bim use offset/limit chunked-read at 2000-line threshold (edit.md, comment.md, error.md, new.md, load.md)

**[4.1.0] — 2026-03-23** (Added + Changed sections):
- Added: Auto-resume — every `/pbi` invocation auto-loads project context from `.pbi-context.md` without requiring explicit `/pbi load`
- Added: LOCAL-FIRST GIT POLICY enforced in all commands — `git pull`, `git fetch`, `git push` are never used; local files are always the source of truth
- Changed: Installer (`install.ps1`) rewritten with `-Scope project|user` parameter, full file manifest including `detect.py` and `docs.md`, dynamic version banner read from downloaded SKILL.md
- Changed: `scripts/` directory created automatically by installer; `detect.py` placed inside it

**[4.0.0] — 2026-03-14** (Added + Changed sections):
- Added: `/pbi docs` command — generates polished, stakeholder-ready model documentation from PBIP project
- Added: `/pbi extract` command with three tiers (overview, standard, deep-dive) for structured project summaries
- Added: `/pbi deep` command — guided multi-phase workflow with upfront context gathering, model review, DAX development, and verification phases
- Changed: Routing table extended to cover `docs`, `extract`, and `deep` keywords
- Changed: Model selection is now automatic — Haiku for file/git operations, Sonnet for DAX reasoning, Opus for deep-dive extraction

**[3.0.0] — 2026-03-14** (Added + Changed sections):
- Added: `/pbi audit` command — full model health check with auto-fix for critical issues (bidirectional filters, unhidden key columns, naming violations)
- Added: `/pbi comment-batch` command — adds descriptions to all undocumented measures in a single pass
- Added: `/pbi changelog` command — generates release notes from recent local git commits
- Added: Parallel agent execution for audit on models with 5+ tables (3 domain-pass agents)
- Added: `/pbi edit` command — modify model entities from plain-English instructions with auto-commit
- Changed: All PBIP commands now use the `PBIP_DIR` value from detection output — never a hardcoded `.SemanticModel`
- Changed: Auto-commit added after successful writes in edit, comment, error, and new; `/pbi undo` reverts the last auto-commit
- Changed: Post-command auto-stage — after every command that writes to `$PBIP_DIR/`, changes are staged with `git add`

**[2.0.0] — 2026-03-14** (Added + Changed sections):
- Added: PBIP context loading — `/pbi load` reads all TMDL/TMSL files, extracts table/measure/column/relationship structure into `.pbi-context.md`
- Added: `/pbi diff` command — summarises uncommitted model changes using `git diff`
- Added: `/pbi commit` command — stages and commits model changes with a generated commit message
- Added: `/pbi undo` command — reverts the last auto-commit using `git revert`
- Added: Session context tracking — every command reads and writes `.pbi-context.md`; Command History is trimmed to 20 rows max
- Changed: Detection blocks run once in SKILL.md and are shared by all subcommands (PBIP detection, file index, PBIR detection, git state)
- Changed: Post-command epilogue added — auto-stage and context tracking (progress bar) after every subcommand

**[1.0.0] — 2026-03-13** (Added section):
- Initial release of `/pbi` skill for Claude Code
- Core subcommands: `explain`, `format`, `optimise`, `comment`, `error`, `new`, `load`, `help`
- DAX Formatter API integration: `format` uses `shared/api-notes.md` for API reference
- Solve-First Default handler: free-text questions route to an immediate DAX solution with a two-failure escalation flow
- Escalation logic: silent retry on first failure, one targeted question on second failure, escalation state written to `.pbi-context.md`
- `detect.py` utility script for UTF-8-safe PBIP/PBIR/git detection

Use the Write tool to create this file. Format each section using standard Keep a Changelog markdown: `## [version] — date`, then `### Added` / `### Changed` subsections with bullet points.
  </action>
  <verify>
    <automated>python -c "
with open('.claude/skills/pbi/shared/CHANGELOG.md', 'r', encoding='utf-8') as f:
    content = f.read()
assert '4.3.0' in content, 'Missing v4.3.0'
assert '1.0.0' in content, 'Missing v1.0.0'
assert 'Keep a Changelog' in content, 'Missing format reference'
assert 'detect.py' in content, 'Missing detect.py mention'
for ver in ['4.1.0', '4.0.0', '3.0.0', '2.0.0']:
    assert ver in content, f'Missing version {ver}'
print('CHANGELOG.md OK - all 6 versions present')
"</automated>
  </verify>
  <done>CHANGELOG.md exists in `.claude/skills/pbi/shared/` with entries for all 6 released versions (v1.0.0 through v4.3.0) in Keep a Changelog format with factual, substantive entries per version.</done>
</task>

<task type="auto">
  <name>Task 2: Create version.md command file</name>
  <files>.claude/skills/pbi/commands/version.md</files>
  <read_first>
    - .claude/skills/pbi/commands/help.md (full file — reference for step structure, bash block pattern, Stop sentinel, Anti-Patterns section)
    - .claude/skills/pbi/scripts/detect.py (lines 178-195 — version_check function and its LOCAL= output format)
  </read_first>
  <action>
Create `.claude/skills/pbi/commands/version.md` modeled after help.md. Follow the same pattern: bash step to gather data, then output step.

Key requirements (per D-02 — changelog only, offline, and PYTHON-FIRST policy):
- Use `detect.py version-check` to read the current version — not grep or sed
- Use the Read tool to load CHANGELOG.md — not bash `cat`
- No network calls — offline only
- End with Stop sentinel and Anti-Patterns section

Write the following file content using the Write tool:

```
# /pbi version

> No detection context needed — this command is self-contained.

## Instructions

### Step 1 - Read current version

Run the following bash command to locate the skill file and read the installed version:

SKILL_FILE=$(find . -path "*/.claude/skills/pbi/SKILL.md" -print -quit 2>/dev/null)
if [ -z "$SKILL_FILE" ]; then SKILL_FILE=$(find "$HOME" -maxdepth 5 -path "*/.claude/skills/pbi/SKILL.md" -print -quit 2>/dev/null); fi
python ".claude/skills/pbi/scripts/detect.py" version-check "$SKILL_FILE" 2>/dev/null || echo "LOCAL=unknown"

Parse the output: LOCAL = installed version (e.g., 4.3.0).

### Step 2 - Read CHANGELOG.md

Use the Read tool to load .claude/skills/pbi/shared/CHANGELOG.md

If the file is not found at that path, try ~/.claude/skills/pbi/shared/CHANGELOG.md

If neither path exists, output:
  PBI Skill - Version LOCAL
  Changelog not available - re-run the installer to download CHANGELOG.md.
Then stop.

### Step 3 - Output version header and changelog

Output the following, substituting LOCAL from Step 1 and the full CHANGELOG.md content from Step 2:

**PBI Skill - Version LOCAL**

[full content of CHANGELOG.md, printed verbatim]

Stop. Do not output anything else.

### Anti-Patterns
- NEVER make network calls or check for updates - this command is offline-only
- NEVER use cat or bash redirection to read CHANGELOG.md - always use the Read tool
- NEVER output anything after the changelog - stop immediately
```

Use the Write tool to create this file. The bash block in Step 1 must be properly fenced with triple backticks in the written file.
  </action>
  <verify>
    <automated>python -c "
with open('.claude/skills/pbi/commands/version.md', 'r', encoding='utf-8') as f:
    content = f.read()
assert 'version-check' in content, 'Missing detect.py version-check call'
assert 'CHANGELOG.md' in content, 'Missing CHANGELOG.md reference'
assert 'Stop.' in content, 'Missing Stop sentinel'
assert 'Anti-Patterns' in content, 'Missing Anti-Patterns section'
assert 'Read tool' in content, 'Must use Read tool, not bash cat'
assert 'network' in content.lower(), 'Missing network call prohibition'
print('version.md OK')
"</automated>
  </verify>
  <done>version.md exists in `.claude/skills/pbi/commands/`. It reads the installed version via `detect.py version-check`, loads CHANGELOG.md via the Read tool, outputs a version header followed by the full changelog, and ends with a Stop sentinel and Anti-Patterns section.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full artifact chain:

```python
import os
changelog = '.claude/skills/pbi/shared/CHANGELOG.md'
version_cmd = '.claude/skills/pbi/commands/version.md'

assert os.path.isfile(changelog), f'Missing: {changelog}'
assert os.path.isfile(version_cmd), f'Missing: {version_cmd}'

with open(changelog, 'r', encoding='utf-8') as f:
    cl = f.read()
for ver in ['4.3.0', '4.1.0', '4.0.0', '3.0.0', '2.0.0', '1.0.0']:
    assert ver in cl, f'Missing version {ver} in CHANGELOG'

with open(version_cmd, 'r', encoding='utf-8') as f:
    vc = f.read()
assert 'version-check' in vc
assert 'CHANGELOG.md' in vc
assert 'Stop.' in vc
assert 'Read tool' in vc

print('Plan 01 verification PASSED')
```
</verification>

<success_criteria>
- `.claude/skills/pbi/shared/CHANGELOG.md` exists with entries for all 6 versions (v1.0.0 through v4.3.0) in Keep a Changelog format
- `.claude/skills/pbi/commands/version.md` exists, reads version via `detect.py version-check`, loads CHANGELOG.md via Read tool, ends with Stop sentinel
- No network calls, no bash cat — Python-first policy followed throughout
</success_criteria>

<output>
After completion, create `.planning/phases/07-version-history-command/07-01-SUMMARY.md`
</output>
